# Casual-EgoSim

This repository contains autoregressive (AR) distillation methods for EgoSim models.

It includes two distillation modes:

- Chunk-wise distillation
- Frame-wise distillation

For usage and runnable examples, see the scripts in [`scripts/`](scripts/).
